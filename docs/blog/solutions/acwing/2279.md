---
title: AcWing 2279. 网络战争
createTime: 2026/08/11 18:34:02
permalink: /blog/acwing/2279/
tags:
    - 网络流
    - 分数规划
---

<LinkCard title="AcWing 2279. 网络战争" href="https://www.acwing.com/problem/content/2281/"/>

## 解法

形式是典型的分数规划，考虑二分一个 $\lambda$，判断当前值与 $\lambda$ 的关系，我们以小于为例：
$$
\frac{\sum w_e}{|C|} < \lambda \implies \left(\sum w_e\right)-|C|\cdot \lambda<0 \implies \sum\left( w_e-\lambda \right)<0
$$
这样变形以后，等价于我们将原图 $G$ 重新赋边权，即 $G\rightarrow G'$，使得 $w_e'=w_e-\lambda$。  
由于原图 $w_e\geq 1$，但减去 $\lambda$ 有可能变成负数，由于负数和 $0$ 不影响答案，考虑把所有 $w_e\leq 0$ 的边提前选上，这样只需要处理 $w_e>0$ 的边。  
接下来直接跑 Dinic 就行。  

## 代码
``` cpp
#include <iostream>
#include <algorithm>
#include <cstdio>
#include <cstring>
#include <cmath>

using namespace std;

const int N = 110 , M = 810;
const double eps = 1e-6 , INF = 2e7;

int n , m , S , T;
int h[N] , e[M] , ne[M] , f[M] , idx; double w[M];
int q[N] , d[N] , cur[N];

void add(int a , int b , int c) {
	f[idx] = c , e[idx] = b , ne[idx] = h[a] , h[a] = idx ++;
	f[idx] = 0 , e[idx] = a , ne[idx] = h[b] , h[b] = idx ++;
}

bool bfs() {
	memset(d , -1 , sizeof d);
	int hh = 0 , tt = -1;
	
	q[++ tt] = S; d[S] = 0; cur[S] = h[S];
	while(hh <= tt) {
		int t = q[hh ++];
		for(int i = h[t] ; ~i ; i = ne[i]) {
			int v = e[i];
			if(d[v] == -1 && w[i] > 0) {
				d[v] = d[t]+1;
				cur[v] = h[v];
				if(v == T) return true;
				q[++ tt] = v;
			}
		}
	}
	return false;
}

double find(int u , double limit) {
	if(u == T) return limit;
	double flow = 0;
	for(int i = cur[u] ; ~i && flow < limit ; i = ne[i]) {
		cur[u] = i;
		int v = e[i];
		if(d[v] == d[u]+1 && w[i] > 0) {
			double t = find(v , min(w[i] , limit-flow));
			if(t < eps) d[v] = -1;
			flow += t; w[i] -= t; w[i^1] += t;
		}
	}
	return flow;
}

double dinic() {
	double r = 0 , flow;
	while(bfs()) while((flow = find(S , INF))) r += flow;
	return r;
}

bool check(double x) {
	double res = 0;
	for(int i = 0 ; i < idx ; i += 2) {
		if(f[i] <= x) res += f[i]-x , w[i] = w[i^1] = 0;
		else w[i] = w[i^1] = f[i]-x;
	}
	return res + dinic() < 0;
}

int main() {
	scanf("%d%d%d%d" , &n , &m , &S , &T);
	memset(h , -1 , sizeof h);
	while(m --) {
		int a , b , c;
		scanf("%d%d%d" , &a , &b , &c);
		add(a , b , c);
	}
	
	double l = 0 , r = 1e7 , mid;
	while(r-l > eps) {
		mid = (l+r) / 2;
		if(check(mid)) r = mid;
		else l = mid;
	}
	printf("%.2lf\n" , r);
	return 0;
}
```